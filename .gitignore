# Project dependencies
.yarn
.cache
*.pyc
node_modules
yarn-error.log

# Build directory
/build
/dist
.DS_Store
.sass-cache/
_posts/
_site/

.mypy_cache

/.vscode
.idea

config.yaml
.venv/
.pdm-python

route.png
route.svg

# Garmin GPX files are temporary sync inputs. Their processed file names are
# persisted in imported.json and the parsed data is persisted in data.db.
GPX_OUT/*.gpx
FIX_OUT/*.fit
FIX_OUT/*.zip

.env
